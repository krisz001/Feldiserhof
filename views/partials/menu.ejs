<%
  // ---- Menü adat biztonságos fallback ----
  const rawMenu = (typeof menuData !== 'undefined' && menuData && typeof menuData === 'object')
    ? menuData
    : {};

  const categories = Array.isArray(rawMenu.categories) ? rawMenu.categories : [];

  // Címsorok – include-ból, menuData-ból, vagy alapértelmezett DE szöveg
  const titleTxt    = (typeof menuTitle    !== 'undefined' && menuTitle)    ? menuTitle    : (rawMenu.title || 'Speisekarte');
  const subtitleTxt = (typeof menuSubtitle !== 'undefined' && menuSubtitle) ? menuSubtitle : 'Entdecken Sie unsere Küche';
  const introTxt    = (typeof menuIntro    !== 'undefined' && menuIntro)    ? menuIntro    : 'Frisch, saisonal, regional – täglich für Sie.';

  // Feature flag-ek (pl. menuBookEnabled)
  const safeFlags = (typeof flags !== 'undefined' && flags) ? flags : {};

  // ---- PDF oldalak fallback ----
  // Ha a controller átadja: res.render(..., { menuPdfPages: [...] })
  // akkor innen vesszük. Ha nem, akkor [] és a JS kérdezi le /api/menu-book-ból.
  const pdfPages = (typeof menuPdfPages !== 'undefined' && Array.isArray(menuPdfPages))
    ? menuPdfPages
    : [];
%>

<section id="menu" class="menu-section">
  <header class="menu-header text-center mb-5">
    <span class="menu-icon" aria-hidden="true">🍴</span>
    <h2 class="menu-title"><%= titleTxt %></h2>
    <div class="menu-divider"></div>
    <p class="menu-subtitle"><%= subtitleTxt %></p>
    <p class="menu-intro"><%= introTxt %></p>
  </header>

  <!-- Könyv wrapper – feature flag + opcionális PDF jelzés -->
  <div
    class="menu-portfolio <%= safeFlags.menuBookEnabled ? '' : 'is-locked' %> <%= pdfPages.length ? 'has-pdf' : '' %>"
    data-menu-book-enabled="<%= safeFlags.menuBookEnabled ? '1' : '0' %>"
    data-pdf-count="<%= pdfPages.length %>"
  >
    <div class="wrapper">

      <!-- 🔒 Ha a menükönyv le van zárva, overlay üzenet -->
      <% if (!safeFlags.menuBookEnabled) { %>
        <div class="book-lock-overlay" role="status" aria-live="polite">
          <div class="blo-card">
            <h3>Wegen Betriebsferien ist unsere Speisekarte momentan nicht verfügbar.</h3>
            <p>Wir freuen uns, Sie bald wieder begrüßen zu dürfen.</p>
          </div>
        </div>
      <% } %>

      <!-- Külső "borító" effektek -->
      <div class="cover cover-left" aria-hidden="true"></div>
      <div class="cover cover-right" aria-hidden="true"></div>

      <!-- Könyv -->
      <div
        class="book"
        id="menuBook"
        <% if (pdfPages && pdfPages.length) { %>
          data-pdf-pages='<%- JSON.stringify(pdfPages).replace(/<\/(script)/gi, "<\\/$1") %>'
        <% } %>
      >
        <!-- Bal nyitó oldal -->
        <div class="book-page page-left">
          <div class="profile-page profile-cover">
            <h1><%= titleTxt %></h1>
            <p><%= subtitleTxt %></p>
          </div>
        </div>

        <!-- Jobb oldal sablon – JS tölti meg tartalommal / PDF képpel (ha nincs PDF, ez sablon a JSON-hoz) -->
        <div class="book-page page-right" data-sheet="1" id="turn-1" data-template="page">
          <div class="page-front">
            <h1 class="title"><%= titleTxt %></h1>

            <!-- Dinamikus menü (JSON-ból) – ha nincs PDF és nincs kategória, ezt látjuk -->
            <div class="menu-items-grid">
              <% if (!categories || !categories.length) { %>
                <article class="menu-item">
                  <p>Derzeit sind keine Menüdaten vorhanden.</p>
                </article>
              <% } %>
            </div>

            <span class="number-page">1</span>

            <span class="nextprev-btn" data-role="next">
              <i class="bx bx-chevron-right"></i>
            </span>
          </div>

          <div class="page-back">
            <h1 class="title"></h1>
            <div class="menu-items-grid"></div>

            <span class="number-page"></span>

            <span class="nextprev-btn back" data-role="prev">
              <i class="bx bx-chevron-left"></i>
            </span>
          </div>
        </div>
      </div>
    </div>

    <!-- Lapozó "dot"-ok -->
    <div class="book-dots"></div>
  </div>

  <!-- Könyv CSS + ikonok -->
  <link rel="stylesheet" href="/css/menu-portfolio-book.css">
  <link rel="stylesheet" href="https://unpkg.com/boxicons@2.1.4/css/boxicons.min.css">

  <!-- Menü JSON beágyazása a dinamikus (nem-PDF) módhoz -->
  <script id="menuDataScript" type="application/json">
    <%- JSON
      .stringify(rawMenu)
      .replace(/<\/(script)/gi, '<\\/$1')
    %>
  </script>

  <!-- PDF oldalak beágyazása (ha a controller átadta) -->
  <script id="pdfMenuPagesScript" type="application/json">
    <%- JSON
      .stringify(pdfPages)
      .replace(/<\/(script)/gi, '<\\/$1')
    %>
  </script>

  <!-- Aktiváló JS PDF módhoz:
       - ha van beágyazott pdfPages → azt használjuk
       - ha nincs, a menu-portfolio-book.js fallback-ként lehívhatja a /api/menu-book-ot
  -->
  <script>
    document.addEventListener('DOMContentLoaded', () => {
      const pdfScriptEl = document.getElementById('pdfMenuPagesScript');
      let pdfPages = [];

      if (pdfScriptEl && pdfScriptEl.textContent.trim()) {
        try {
          pdfPages = JSON.parse(pdfScriptEl.textContent.trim());
        } catch (e) {
          console.error('PDF pages JSON parse error in menu-book.ejs:', e);
        }
      }

      if (Array.isArray(pdfPages) && pdfPages.length > 0) {
        // Globális változó a menu-portfolio-book.js számára (extra biztosíték)
        window.menuPdfPages = pdfPages;
        document.body.dataset.pdfMenuActive = '1';
      }
    });
  </script>
</section>

<div class="section-separator"></div>

<style>
  .menu-portfolio .wrapper {
    position: relative;
  }

  .menu-portfolio.is-locked .cover,
  .menu-portfolio.is-locked .book {
    filter: blur(14px) brightness(0.85) saturate(0.9);
    pointer-events: none !important;
    user-select: none !important;
  }

  .book-lock-overlay {
    position: absolute;
    inset: 0;
    display: flex;
    align-items: center;
    justify-content: center;
    padding: 24px;
    background: radial-gradient(ellipse at center, rgba(0,0,0,.18), rgba(0,0,0,.28));
    z-index: 999;
  }

  .book-lock-overlay .blo-card {
    max-width: 720px;
    width: 100%;
    text-align: center;
    padding: 28px 24px;
    border-radius: 16px;
    background: rgba(255,255,255,.9);
    box-shadow: 0 20px 60px rgba(0,0,0,.25);
    backdrop-filter: blur(4px);
  }

  .book-lock-overlay .blo-card h3 {
    margin: 0 0 8px;
    font-weight: 700;
    font-size: clamp(1.1rem, 2.2vw, 1.5rem);
    color: #2b2b2b;
  }

  .book-lock-overlay .blo-card p {
    margin: 0;
    font-size: clamp(.95rem, 2vw, 1.05rem);
    color: #444;
  }
</style>
