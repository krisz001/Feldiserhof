<section id="gallery" class="container-xxl py-5">
  <div class="container">
    <!-- Fejléc -->
    <div class="gallery-head text-center mb-4">
      <span class="kicker">Galerie</span>
      <h2 class="title">Einblicke in den Feldiserhof</h2>
      <!-- (opcionális) rövid leírás
      <p class="lead">Ein paar Eindrücke aus Hotel, Restaurant und Feldis.</p>
      -->
    </div>

    <!-- ide rajzol a gallery.js -->
    <div id="galleryView"></div>
  </div>

  <!-- Lightbox Modal -->
  <div class="modal fade" id="lightbox" tabindex="-1" aria-hidden="true">
    <div class="modal-dialog modal-dialog-centered modal-lg">
      <div class="modal-content bg-dark text-white border-0">
        <div class="modal-body text-center">
          <img id="lightboxImg" class="img-fluid rounded" alt="">
          <p id="lightboxCap" class="mt-2 small text-muted"></p>
        </div>
      </div>
    </div>
  </div>
</section>
<div class="section-separator"></div>