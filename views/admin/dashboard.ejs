<%- include('../partials/head', { 
  title: t('admin.title'),
  description: t('admin.description')
}) %>

<%- include('../partials/navbar') %>

<main class="container py-5 mt-5">
  <div class="row">
    <div class="col-12">
      <div class="d-flex justify-content-between align-items-center mb-4">
        <div class="d-flex align-items-center">
          <h1 class="h2 text-primary mb-0">
            <i class="fas fa-crown me-2"></i>
            <span id="dashboard-title"><%= t('admin.dashboardTitle') %></span>
          </h1>

          <!-- Nyelvválasztó - admin session megőrzésével -->
          <div class="dropdown ms-3">
            <button class="btn btn-outline-secondary dropdown-toggle" type="button" id="languageDropdown" data-bs-toggle="dropdown" aria-expanded="false">
              <i class="fas fa-globe me-1"></i>
              <span id="current-language"><%- (i18n.language === 'de' ? 'Deutsch' : 'Magyar') %></span>
            </button>
            <ul class="dropdown-menu" aria-labelledby="languageDropdown">
              <li>
                <a class="dropdown-item language-option <%- i18n.language === 'hu' ? 'active' : '' %>" href="/set-language/hu?admin=true" rel="nofollow">
                  <i class="flag-icon flag-icon-hu me-2"></i><span>Magyar</span>
                </a>
              </li>
              <li>
                <a class="dropdown-item language-option <%- i18n.language === 'de' ? 'active' : '' %>" href="/set-language/de?admin=true" rel="nofollow">
                  <i class="flag-icon flag-icon-de me-2"></i><span>Deutsch</span>
                </a>
              </li>
            </ul>
          </div>
        </div>

        <form id="logoutForm" method="post" action="/admin/logout">
          <input type="hidden" name="_csrf" value="<%= typeof csrfToken !== 'undefined' ? csrfToken : '' %>">
          <button class="btn btn-outline-danger" id="logout-button" type="submit">
            <i class="fas fa-sign-out-alt me-1"></i>
            <span id="logout-text"><%= t('admin.logout') %></span>
          </button>
        </form>
      </div>

      <div class="alert alert-success d-flex align-items-center" id="welcome-alert" role="status" aria-live="polite">
        <i class="fas fa-check-circle me-2 fs-5"></i>
        <div>
          <span id="welcome-text"><%= t('admin.welcome') %></span>
        </div>
      </div>

      <div class="row mt-4">
        <!-- Hero Box szerkesztése -->
        <div class="col-md-6 col-lg-4 mb-4">
          <div class="card border-primary h-100">
            <div class="card-body d-flex flex-column">
              <h5 class="card-title text-primary">
                <i class="fas fa-mountain me-2"></i>
                <span>Hero Box Kezelő</span>
              </h5>
              <p class="card-text flex-grow-1">A főoldali hero szekcióban megjelenő box tartalmának szerkesztése</p>
              <button class="btn btn-primary mt-auto" onclick="openHeroBoxEditor()">Szerkesztés</button>
            </div>
          </div>
        </div>

        <!-- Menü szerkesztése -->
        <div class="col-md-6 col-lg-4 mb-4">
          <div class="card border-primary h-100">
            <div class="card-body d-flex flex-column">
              <h5 class="card-title text-primary">
                <i class="fas fa-utensils me-2"></i>
                <span id="menu-editor-title"><%= t('admin.menuEditor') %></span>
              </h5>
              <p class="card-text flex-grow-1" id="menu-editor-desc"><%= t('admin.menuEditorDesc') %></p>
              <a href="/admin/menu" class="btn btn-primary mt-auto" id="menu-editor-button"><%= t('admin.open') %></a>
            </div>
          </div>
        </div>

        <!-- Galéria kezelése -->
        <div class="col-md-6 col-lg-4 mb-4">
          <div class="card border-success h-100">
            <div class="card-body d-flex flex-column">
              <h5 class="card-title text-success">
                <i class="fas fa-image me-2"></i>
                <span id="gallery-title"><%= t('admin.gallery') %></span>
              </h5>
              <p class="card-text flex-grow-1" id="gallery-desc"><%= t('admin.galleryDesc') %></p>
              <a href="/admin/gallery" class="btn btn-success mt-auto" id="gallery-button"><%= t('admin.open') %></a>
            </div>
          </div>
        </div>

        <!-- Foglalások -->
        <div class="col-md-6 col-lg-4 mb-4">
          <div class="card border-info h-100">
            <div class="card-body d-flex flex-column">
              <h5 class="card-title text-info">
                <i class="fas fa-calendar-alt me-2"></i>
                <span id="bookings-title"><%= t('admin.bookings') %></span>
              </h5>
              <p class="card-text flex-grow-1" id="bookings-desc"><%= t('admin.bookingsDesc') %></p>
              <a href="/admin/bookings" class="btn btn-info mt-auto" id="bookings-button"><%= t('admin.open') %></a>
            </div>
          </div>
        </div>

        <!-- Nyitvatartás -->
        <div class="col-md-6 col-lg-4 mb-4">
          <div class="card border-warning h-100">
            <div class="card-body d-flex flex-column">
              <h5 class="card-title text-warning">
                <i class="fas fa-clock me-2"></i>
                <span id="hours-title"><%= t('admin.hours') %></span>
              </h5>
              <p class="card-text flex-grow-1" id="hours-desc"><%= t('admin.hoursDesc') %></p>
              <a href="/admin/hours" class="btn btn-warning mt-auto" id="hours-button"><%= t('admin.open') %></a>
            </div>
          </div>
        </div>

        <!-- Alkalmazottak -->
        <div class="col-md-6 col-lg-4 mb-4">
          <div class="card border-secondary h-100">
            <div class="card-body d-flex flex-column">
              <h5 class="card-title text-secondary">
                <i class="fas fa-users me-2"></i>
                <span id="staff-title"><%= t('admin.staff') %></span>
              </h5>
              <p class="card-text flex-grow-1" id="staff-desc"><%= t('admin.staffDesc') %></p>
              <a href="/admin/staff" class="btn btn-secondary mt-auto" id="staff-button"><%= t('admin.open') %></a>
            </div>
          </div>
        </div>

        <!-- Statisztikák -->
        <div class="col-md-6 col-lg-4 mb-4">
          <div class="card border-dark h-100">
            <div class="card-body d-flex flex-column">
              <h5 class="card-title text-dark">
                <i class="fas fa-chart-bar me-2"></i>
                <span id="analytics-title"><%= t('admin.analytics') %></span>
              </h5>
              <p class="card-text flex-grow-1" id="analytics-desc"><%= t('admin.analyticsDesc') %></p>
              <a href="/admin/analytics" class="btn btn-dark mt-auto" id="analytics-button"><%= t('admin.open') %></a>
            </div>
          </div>
        </div>

        <!-- Feature-Schalter (Mitarbeitende) -->
        <div class="col-md-6 col-lg-4 mb-4">
          <div class="card border-secondary h-100">
            <div class="card-body d-flex flex-column">
              <h5 class="card-title text-secondary">
                <i class="fas fa-toggle-on me-2"></i>
                <span>Feature-Schalter</span>
              </h5>
              <p class="card-text flex-grow-1">
                „Speisekarte – Buch öffnen" engedélyezése/tiltása.
              </p>

              <div class="mb-3">
                <% if (flags && flags.menuBookEnabled) { %>
                  <span id="flagBadge" class="badge bg-success">Könyv nyitása: engedélyezve</span>
                <% } else { %>
                  <span id="flagBadge" class="badge bg-secondary">Könyv nyitása: tiltva</span>
                <% } %>
              </div>

              <a href="/admin/mitarbeitende" class="btn btn-secondary mt-auto">
                <i class="fas fa-arrow-right me-1"></i> Megnyitás
              </a>
            </div>
          </div>
        </div>
      </div>
    </div>
  </div>
</main>

<!-- Hero Box Szerkesztő Modal -->
<div class="modal fade" id="heroBoxModal" tabindex="-1" aria-labelledby="heroBoxModalLabel" aria-hidden="true">
  <div class="modal-dialog modal-lg">
    <div class="modal-content">
      <div class="modal-header">
        <h5 class="modal-title" id="heroBoxModalLabel">
          <i class="fas fa-mountain me-2"></i>Hero Box Szerkesztése
        </h5>
        <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"></button>
      </div>
      <div class="modal-body">
        <div class="row">
          <div class="col-md-6">
            <!-- Enabled kapcsoló -->
            <div class="mb-3 form-check form-switch">
              <input type="checkbox" class="form-check-input" id="heroBoxEnabled" checked>
              <label class="form-check-label" for="heroBoxEnabled">Aktiválva</label>
            </div>

            <!-- Icon -->
            <div class="mb-3">
              <label for="heroBoxIcon" class="form-label">
                <i class="fas fa-icons me-1"></i>Ikon (emoji)
              </label>
              <input type="text" class="form-control" id="heroBoxIcon" placeholder="🏔️" maxlength="2">
            </div>

            <!-- Title -->
            <div class="mb-3">
              <label for="heroBoxTitle" class="form-label">
                <i class="fas fa-heading me-1"></i>Cím
              </label>
              <input type="text" class="form-control" id="heroBoxTitle" placeholder="Aktuelles Angebot" required>
            </div>

            <!-- Description -->
            <div class="mb-3">
              <label for="heroBoxDescription" class="form-label">
                <i class="fas fa-align-left me-1"></i>Leírás
              </label>
              <textarea class="form-control" id="heroBoxDescription" rows="3" placeholder="Genießen Sie unseren speziellen Bergblick mit 3-Gänge-Menü" required></textarea>
            </div>

            <!-- Button Text -->
            <div class="mb-3">
              <label for="heroBoxButtonText" class="form-label">
                <i class="fas fa-mouse-pointer me-1"></i>Gomb szöveg
              </label>
              <input type="text" class="form-control" id="heroBoxButtonText" placeholder="Mehr erfahren">
            </div>

            <!-- Button Link -->
            <div class="mb-3">
              <label for="heroBoxButtonLink" class="form-label">
                <i class="fas fa-link me-1"></i>Gomb link
              </label>
              <input type="text" class="form-control" id="heroBoxButtonLink" placeholder="#offers">
            </div>

            <!-- Start Date -->
            <div class="mb-3">
              <label for="heroBoxStartDate" class="form-label">
                <i class="fas fa-calendar-check me-1"></i>Kezdő dátum
              </label>
              <input type="date" class="form-control" id="heroBoxStartDate">
            </div>

            <!-- End Date -->
            <div class="mb-3">
              <label for="heroBoxEndDate" class="form-label">
                <i class="fas fa-calendar-times me-1"></i>Befejező dátum
              </label>
              <input type="date" class="form-control" id="heroBoxEndDate">
            </div>

            <!-- Priority -->
            <div class="mb-3">
              <label for="heroBoxPriority" class="form-label">
                <i class="fas fa-sort-numeric-up me-1"></i>Prioritás
              </label>
              <input type="number" class="form-control" id="heroBoxPriority" value="1" min="1">
            </div>

            <!-- Target Audience -->
            <div class="mb-3">
              <label for="heroBoxTargetAudience" class="form-label">
                <i class="fas fa-users me-1"></i>Célközönség
              </label>
              <select class="form-select" id="heroBoxTargetAudience">
                <option value="all">Mindenki</option>
                <option value="restaurant">Étterem</option>
                <option value="hotel">Hotel</option>
                <option value="wellness">Wellness</option>
              </select>
            </div>

            <!-- Is Active -->
            <div class="mb-3 form-check form-switch">
              <input type="checkbox" class="form-check-input" id="heroBoxIsActive" checked>
              <label class="form-check-label" for="heroBoxIsActive">Aktív állapot</label>
            </div>
          </div>

          <!-- Preview -->
          <div class="col-md-6">
            <h6 class="mb-3"><i class="fas fa-eye me-1"></i>Előnézet</h6>
            <div id="heroBoxPreview" class="p-4 bg-light rounded border">
              <div class="d-flex align-items-start">
                <span id="previewIcon" class="fs-1 me-3">🏔️</span>
                <div class="flex-grow-1">
                  <h5 id="previewTitle" class="mb-2">Aktuelles Angebot</h5>
                  <p id="previewDescription" class="mb-3">Genießen Sie unseren speziellen Bergblick mit 3-Gänge-Menü</p>
                  <a href="#" id="previewButton" class="btn btn-primary btn-sm">Mehr erfahren</a>
                  <div class="mt-2">
                    <small id="previewDates" class="text-muted">Gültig bis: 25.11.2025</small>
                  </div>
                </div>
              </div>
            </div>
          </div>
        </div>
      </div>
      <div class="modal-footer">
        <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">
          <i class="fas fa-times me-1"></i>Mégse
        </button>
        <button type="button" class="btn btn-primary" onclick="saveHeroBox()">
          <i class="fas fa-save me-1"></i>Mentés
        </button>
      </div>
    </div>
  </div>
</div>

<%- include('../partials/footer') %>

<style>
.flag-icon{width:20px;height:15px;display:inline-block;background-size:cover;border-radius:2px}
.flag-icon-hu{background-image:url('data:image/svg+xml;utf8,<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 3 2"><path fill="%23CE2939" d="M0 0h3v0.6667H0z"/><path fill="%23FFFFFF" d="M0 0.6667h3v0.6666H0z"/><path fill="%2300903B" d="M0 1.3333h3V2H0z"/></svg>')}
.flag-icon-de{background-image:url('data:image/svg+xml;utf8,<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 3 2"><path fill="%23000000" d="M0 0h3v0.6667H0z"/><path fill="%23DD0000" d="M0 0.6667h3v0.6666H0z"/><path fill="%23FFCE00" d="M0 1.3333h3V2H0z"/></svg>')}
.dropdown-item.active{background-color:#e9ecef}
#heroBoxPreview{border-left:4px solid #ffc107}
</style>

<script>
// CSRF token a többi admin művelethez
let csrfToken = '';
(async () => {
  try {
    const resp = await fetch('/api/csrf-token', { method: 'GET', credentials: 'same-origin', headers: { 'Accept': 'application/json' } });
    if (!resp.ok) throw new Error('CSRF fetch failed: ' + resp.status);
    const data = await resp.json();
    csrfToken = data.token || '';
    document.querySelectorAll('input[name="_csrf"]').forEach(i => i.value = csrfToken);
  } catch (err) {
    console.error('❌ CSRF token lekérési hiba:', err);
  }
})();

// Feature-Schalter jelvény frissítése betöltéskor
document.addEventListener('DOMContentLoaded', async () => {
  const badge = document.getElementById('flagBadge');
  if (!badge) return;
  try {
    const r = await fetch('/api/feature-flags', { credentials: 'same-origin' });
    if (!r.ok) return;
    const j = await r.json();
    const on = !!j.menuBookEnabled;
    badge.textContent = on ? 'Könyv nyitása: engedélyezve' : 'Könyv nyitása: tiltva';
    badge.classList.toggle('bg-success', on);
    badge.classList.toggle('bg-secondary', !on);
  } catch {}
});

// Hero Box funkciók
let currentHeroBoxData = {};

async function getCsrfToken() {
  if (!csrfToken) {
    try {
      const r = await fetch('/api/csrf-token');
      const d = await r.json();
      csrfToken = d.token;
    } catch (e) {
      console.error('CSRF token lekérési hiba:', e);
    }
  }
  return csrfToken;
}

function updatePreview() {
  const iconEl = document.getElementById('previewIcon');
  const titleEl = document.getElementById('previewTitle');
  const descEl = document.getElementById('previewDescription');
  const btnEl = document.getElementById('previewButton');
  const datesEl = document.getElementById('previewDates');

  if (iconEl) iconEl.textContent = document.getElementById('heroBoxIcon')?.value || '🏔️';
  if (titleEl) titleEl.textContent = document.getElementById('heroBoxTitle')?.value || 'Aktuelles Angebot';
  if (descEl) descEl.textContent = document.getElementById('heroBoxDescription')?.value || 'Genießen Sie unseren speziellen Bergblick mit 3-Gänge-Menü';
  if (btnEl) btnEl.textContent = document.getElementById('heroBoxButtonText')?.value || 'Mehr erfahren';

  const endDate = document.getElementById('heroBoxEndDate')?.value;
  if (datesEl) {
    datesEl.textContent = endDate
      ? `Gültig bis: ${new Date(endDate).toLocaleDateString('de-DE')}`
      : 'Gültig bis: 25.11.2025';
  }
}

document.addEventListener('DOMContentLoaded', function() {
  ['heroBoxIcon', 'heroBoxTitle', 'heroBoxDescription', 'heroBoxButtonText', 'heroBoxEndDate']
    .forEach(id => {
      const el = document.getElementById(id);
      if (el) el.addEventListener('input', updatePreview);
    });
});

async function openHeroBoxEditor() {
  try {
    const response = await fetch('/admin/api/hero-box', {
      credentials: 'same-origin',
      headers: { 'CSRF-Token': await getCsrfToken() }
    });

    if (!response.ok) throw new Error('Failed to load hero box data');

    currentHeroBoxData = await response.json();

    // Csak akkor állítunk be értéket, ha az elem létezik
    const enabledEl = document.getElementById('heroBoxEnabled');
    const iconEl = document.getElementById('heroBoxIcon');
    const titleEl = document.getElementById('heroBoxTitle');
    const descEl = document.getElementById('heroBoxDescription');
    const btnTextEl = document.getElementById('heroBoxButtonText');
    const btnLinkEl = document.getElementById('heroBoxButtonLink');
    const startDateEl = document.getElementById('heroBoxStartDate');
    const endDateEl = document.getElementById('heroBoxEndDate');
    const priorityEl = document.getElementById('heroBoxPriority');
    const targetEl = document.getElementById('heroBoxTargetAudience');
    const activeEl = document.getElementById('heroBoxIsActive');

    if (enabledEl) enabledEl.checked = currentHeroBoxData.enabled || false;
    if (iconEl) iconEl.value = currentHeroBoxData.icon || '';
    if (titleEl) titleEl.value = currentHeroBoxData.title || '';
    if (descEl) descEl.value = currentHeroBoxData.description || '';
    if (btnTextEl) btnTextEl.value = currentHeroBoxData.buttonText || '';
    if (btnLinkEl) btnLinkEl.value = currentHeroBoxData.buttonLink || '';
    if (startDateEl) startDateEl.value = currentHeroBoxData.startDate || '';
    if (endDateEl) endDateEl.value = currentHeroBoxData.endDate || '';
    if (priorityEl) priorityEl.value = currentHeroBoxData.priority || 1;
    if (targetEl) targetEl.value = currentHeroBoxData.targetAudience || 'all';
    if (activeEl) activeEl.checked = currentHeroBoxData.isActive !== false;

    updatePreview();

    const modal = new bootstrap.Modal(document.getElementById('heroBoxModal'));
    modal.show();
  } catch (e) {
    console.error('Hiba a hero box betöltésekor:', e);
    alert('Hiba a hero box adatok betöltésekor.');
  }
}

async function saveHeroBox() {
  try {
    const formData = {
      enabled: document.getElementById('heroBoxEnabled')?.checked || false,
      icon: document.getElementById('heroBoxIcon')?.value || '',
      title: document.getElementById('heroBoxTitle')?.value || '',
      description: document.getElementById('heroBoxDescription')?.value || '',
      buttonText: document.getElementById('heroBoxButtonText')?.value || '',
      buttonLink: document.getElementById('heroBoxButtonLink')?.value || '',
      startDate: document.getElementById('heroBoxStartDate')?.value || '',
      endDate: document.getElementById('heroBoxEndDate')?.value || '',
      priority: parseInt(document.getElementById('heroBoxPriority')?.value) || 1,
      targetAudience: document.getElementById('heroBoxTargetAudience')?.value || 'all',
      isActive: document.getElementById('heroBoxIsActive')?.checked !== false
    };

    if (!formData.title.trim()) return alert('A cím megadása kötelező!');
    if (!formData.description.trim()) return alert('A leírás megadása kötelező!');

    const response = await fetch('/admin/api/hero-box', {
      method: 'POST',
      credentials: 'same-origin',
      headers: {
        'Content-Type': 'application/json',
        'CSRF-Token': await getCsrfToken()
      },
      body: JSON.stringify(formData)
    });

    if (!response.ok) throw new Error('Failed to save hero box data');

    const result = await response.json();
    if (result.ok) {
      const modal = bootstrap.Modal.getInstance(document.getElementById('heroBoxModal'));
      if (modal) modal.hide();
      alert('Hero Box sikeresen mentve!');
    } else {
      alert('Hiba a mentés során: ' + (result.msg || 'Ismeretlen hiba'));
    }
  } catch (e) {
    console.error('Hiba a hero box mentésekor:', e);
    alert('Hiba a hero box mentésekor.');
  }
}
</script>
