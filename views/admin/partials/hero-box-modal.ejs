<!-- Quill stylesheet and script (helyezd a global page head/foot-ba is) -->
<link href="https://cdn.quilljs.com/1.3.6/quill.snow.css" rel="stylesheet">
<script src="https://cdn.quilljs.com/1.3.6/quill.min.js"></script>

<div class="modal fade" id="heroBoxModal" tabindex="-1" aria-labelledby="heroBoxModalLabel" aria-hidden="true">
  <div class="modal-dialog modal-xl">
    <div class="modal-content border-0 shadow-lg">

      <div class="modal-header bg-primary text-white">
        <h5 class="modal-title fw-bold" id="heroBoxModalLabel">
          <i class="fas fa-mountain me-2"></i>Hero Box szerkesztő
        </h5>
        <button type="button" class="btn-close btn-close-white" data-bs-dismiss="modal" aria-label="Close"></button>
      </div>

      <div class="modal-body py-4">
        <div class="row">

          <!-- Bal oldal: szerkesztés -->
          <div class="col-lg-7 pe-lg-5 mb-4 mb-lg-0">
            <div class="mb-4">
              <h6 class="fw-semibold border-bottom pb-2 mb-3 text-primary">Alapbeállítások</h6>
              <!-- Enabled kapcsoló -->
              <div class="form-check form-switch my-2">
                <input type="checkbox" class="form-check-input" id="heroBoxEnabled">
                <label class="form-check-label" for="heroBoxEnabled">Aktiválva</label>
              </div>
              <!-- Ikon, Cím -->
              <div class="row g-3 mt-2">
                <div class="col-4">
                  <label for="heroBoxIcon" class="form-label small">Ikon (emoji)</label>
                  <input type="text" class="form-control" id="heroBoxIcon" maxlength="2">
                </div>
                <div class="col-8">
                  <label for="heroBoxTitle" class="form-label small">Cím</label>
                  <input type="text" class="form-control" id="heroBoxTitle" required>
                </div>
              </div>
              <!-- Leírás szerkesztő + Event címke -->
              <div class="mt-3">
                <label class="form-label small">Leírás vagy esemény (formázható)</label>
                <div id="heroBoxDescriptionEditor" style="height: 150px; background: #fff; border-radius:8px;"></div>
                <input type="hidden" id="heroBoxDescription" required>
                <small class="text-muted mt-1 d-block">Félkövér, dőlt, lista, link, sortörés támogatott.</small>
              </div>
              <!-- Opcionális esemény címke mező -->
              <div class="mt-3">
                <label for="heroBoxEventLabel" class="form-label small">Esemény címke (pl. "Event")</label>
                <input type="text" class="form-control" id="heroBoxEventLabel" placeholder="Event">
                <small class="text-muted mt-1 d-block">Ha kitöltöd, külön kiemelő sávban jelenik meg a leírás tetején.</small>
              </div>
            </div>

            <!-- Kiemelések, események -->
            <div class="mb-4">
              <h6 class="fw-semibold border-bottom pb-2 mb-3 text-primary">Kiemelések és események</h6>
              <label for="heroBoxHighlightText" class="form-label small">Kiemelt sorok (egy sor=egy esemény)</label>
              <textarea class="form-control mb-2" id="heroBoxHighlightText" rows="3"></textarea>
              <small class="text-muted">Új sor új eseményként jelenik meg.</small>
            </div>

            <!-- Részletes adatok -->
            <div class="mb-4">
              <h6 class="fw-semibold border-bottom pb-2 mb-3 text-primary">Részletes adatok</h6>
              <div class="row g-3">
                <div class="col-6">
                  <label for="heroBoxBottomLabel" class="form-label small">Alsó infósáv</label>
                  <input type="text" class="form-control" id="heroBoxBottomLabel">
                </div>
                <div class="col-6">
                  <label for="heroBoxButtonText" class="form-label small">Gomb szöveg</label>
                  <input type="text" class="form-control" id="heroBoxButtonText">
                </div>
                <div class="col-6">
                  <label for="heroBoxButtonLink" class="form-label small">Gomb link</label>
                  <input type="text" class="form-control" id="heroBoxButtonLink">
                </div>
                <div class="col-3">
                  <label for="heroBoxStartDate" class="form-label small">Kezdő dátum</label>
                  <input type="date" class="form-control" id="heroBoxStartDate">
                </div>
                <div class="col-3">
                  <label for="heroBoxEndDate" class="form-label small">Befejező dátum</label>
                  <input type="date" class="form-control" id="heroBoxEndDate">
                </div>
              </div>
            </div>

            <!-- Prioritás, célközönség, design -->
            <div>
              <h6 class="fw-semibold border-bottom pb-2 mb-3 text-primary">Prioritás és design</h6>
              <div class="row g-3">
                <div class="col-4">
                  <label for="heroBoxPriority" class="form-label small">Prioritás</label>
                  <input type="number" class="form-control" id="heroBoxPriority" min="1">
                </div>
                <div class="col-8">
                  <label for="heroBoxTargetAudience" class="form-label small">Célközönség</label>
                  <select class="form-select" id="heroBoxTargetAudience">
                    <option value="all">Mindenki</option>
                    <option value="restaurant">Étterem</option>
                    <option value="hotel">Hotel</option>
                    <option value="wellness">Wellness</option>
                  </select>
                </div>
                <div class="col-4">
                  <label for="heroBoxStyle" class="form-label small">Kártya stílus</label>
                  <select class="form-select" id="heroBoxStyle">
                    <option value="glass">Glass</option>
                    <option value="simple">Matt</option>
                    <option value="bordered">Keretes</option>
                  </select>
                </div>
                <div class="col-4">
                  <label for="heroBoxTheme" class="form-label small">Szín téma</label>
                  <select class="form-select" id="heroBoxTheme">
                    <option value="gold">Arany</option>
                    <option value="green">Zöld</option>
                    <option value="blue">Kék</option>
                  </select>
                </div>
                <div class="col-4">
                  <label for="heroBoxAlign" class="form-label small">Szöveg igazítás</label>
                  <select class="form-select" id="heroBoxAlign">
                    <option value="center">Középre</option>
                    <option value="left">Balra</option>
                  </select>
                </div>
              </div>
              <div class="form-check form-switch mt-3">
                <input type="checkbox" class="form-check-input" id="heroBoxIsActive">
                <label class="form-check-label" for="heroBoxIsActive">Aktív állapot</label>
              </div>
            </div>
          </div>

          <!-- Jobb oldal: előnézet -->
          <div class="col-lg-5">
            <div class="sticky-top py-2">
              <h6 class="fw-semibold mb-3 text-primary">
                <i class="fas fa-eye me-1"></i>Előnézet
              </h6>
              <div id="heroBoxPreview" class="p-4 bg-light rounded border shadow-sm hero-preview-style-glass hero-preview-theme-gold hero-preview-align-center">
                <div class="d-flex align-items-start">
                  <span id="previewIcon" class="fs-1 me-3">🏔️</span>
                  <div class="flex-grow-1">
                    <h5 id="previewTitle" class="mb-2">Aktuelles Angebot</h5>
                    <!-- Event (opcionális) -->
                    <p id="previewEventLabel" class="text-primary fw-bold mb-1"></p>
                    <p id="previewDescription" class="mb-3"></p>
                    <a href="#" id="previewButton" class="btn btn-primary btn-sm">Mehr erfahren</a>
                    <div class="mt-2">
                      <small id="previewDates" class="text-muted"></small>
                    </div>
                  </div>
                </div>
              </div>
            </div>
          </div>

        </div>
      </div>

      <div class="modal-footer bg-light">
        <button type="button" class="btn btn-outline-secondary" data-bs-dismiss="modal">
          <i class="fas fa-times me-1"></i>Mégse
        </button>
        <button type="button" class="btn btn-primary px-4" onclick="saveHeroBox()">
          <i class="fas fa-save me-2"></i>Mentés
        </button>
      </div>

    </div>
  </div>
</div>
